_subdirectory: template
_tasks:
    # Removed until charm.py sets a status
    # - invoke migrate-test-charm
    - invoke check-yaml
      # - invoke black
      # - invoke remove-tasks
_skip_if_exists:
    - src/charm.py
charm_name:
    type: str
    help: "The name of the charm, as used in metadata.yaml"
class_name:
    type: str
    help: "The class name for the charm, must be a valid python class name"
